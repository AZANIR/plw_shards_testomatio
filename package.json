{
  "name": "examplash-playwright-testomat",
  "version": "1.0.0",
  "description": "Playwright tests with Testomat.io reporter and sharded CI runs",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@testomatio/reporter": "^2.3.8"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

